<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="10dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="8dp"
    app:strokeColor="@android:color/darker_gray"
    app:strokeWidth="0.5dp"
    app:cardUseCompatPadding="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="12dp">

        <!-- Gambar Penginapan -->
        <FrameLayout
            android:layout_width="110dp"
            android:layout_height="110dp"
            android:clipToOutline="true">

            <ImageView
                android:id="@+id/ivPenginapan"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                android:contentDescription="Gambar Penginapan"
                android:src="@drawable/ic_launcher_background"
                android:background="@drawable/rounded_image_bg" />

            <!-- Overlay gradient opsional -->
            <View
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@drawable/gradient_overlay_top" />
        </FrameLayout>

        <!-- Info Penginapan -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:orientation="vertical"
            android:paddingStart="12dp">

            <!-- Nama -->
            <TextView
                android:id="@+id/tvNamaPenginapan"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Hotel Bintang Lima"
                android:textStyle="bold"
                android:textSize="18sp"
                android:textColor="@android:color/black"
                android:ellipsize="end"
                android:maxLines="1" />

            <!-- Alamat -->
            <TextView
                android:id="@+id/tvAlamatPenginapan"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Jl. Pantai Indah No. 123"
                android:textSize="13sp"
                android:textColor="@android:color/darker_gray"
                android:layout_marginTop="2dp" />

            <!-- Harga -->
            <TextView
                android:id="@+id/tvHargaPenginapan"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Rp 500.000 / malam"
                android:textSize="14sp"
                android:textStyle="bold"
                android:textColor="@android:color/holo_red_dark"
                android:layout_marginTop="6dp" />

            <!-- Rating dan tombol -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:layout_marginTop="4dp">

                <RatingBar
                    android:id="@+id/ratingBarPenginapan"
                    style="@style/Widget.AppCompat.RatingBar.Small"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:numStars="5"
                    android:stepSize="0.5"
                    android:isIndicator="true"
                    android:rating="4.5" />

                <!--TextView
                    android:id="@+id/btnDetailPenginapan"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="16dp"
                    android:padding="6dp"
                    android:background="@drawable/bg_button_round"
                    android:text="Detail"
                    android:textSize="12sp"
                    android:textColor="@android:color/white" /-->
            </LinearLayout>
        </LinearLayout>
    </LinearLayout>
</androidx.cardview.widget.CardView>
